import { colors } from './colors';
import { fonts } from './fonts';
import { breakPoint } from './breakPoint';
import { a11y } from './a11y';
import { zIndex } from './zIndex';

const themes = { colors, fonts, breakPoint, a11y, zIndex };

export default themes;
